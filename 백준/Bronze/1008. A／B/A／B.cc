#define ll long long
#include <iostream>
using namespace std;

int main() {
  ios::sync_with_stdio(0);
  cin.tie(0);
  cout << fixed;
  cout.precision(10);
  double a, b;
  cin >> a >> b;
  cout << a / b;
}